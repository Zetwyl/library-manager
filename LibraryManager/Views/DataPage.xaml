<Page x:Class="LibraryManager.Views.DataPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LibraryManager.Views"
      Loaded="Page_Loaded"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DataPage">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Поиск (Читатель):" VerticalAlignment="Center" Margin="5"/>
            <TextBox x:Name="search" Width="140" Height="25"/>
            <Button Click="Search_Click" Content="Поиск" Width="70" Height="25" Margin="5"/>

            <Button Click="Filter_Click" Content="Показать должников" Width="140" Height="25" Margin="5"/>
            <Button Click="Sort_Click" Content="Сортировка по дате" Width="140" Height="25" Margin="5"/>
            <Button Click="Reset_Click" Content="Сбросить" Width="70" Height="25"/>
        </StackPanel>

        <DataGrid Grid.Row="1"
                  IsReadOnly="True"
                  AutoGenerateColumns="False"
                  x:Name="UsersGrid">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Width="*" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Читатель" Width="*" Binding="{Binding ReaderName}"/>
                <DataGridTextColumn Header="Книга" Width="*" Binding="{Binding Book.Title}"/>
                <DataGridTextColumn Header="Дата выдачи" Width="*" Binding="{Binding IssueDate, StringFormat=\{0:dd.MM.yyyy\}}"/>
                <DataGridTextColumn Header="Дата возврата" Width="*" Binding="{Binding ReturnDate, StringFormat=\{0:dd.MM.yyyy\}, TargetNullValue='На руках'}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" HorizontalAlignment="Center" Orientation="Horizontal" VerticalAlignment="Center">
            <Button Height="24" Click="Add_Click" Content="Выдать книгу" Width="150"/>
            <Button Height="24" Click="Edit_Click" Content="Редактировать" Width="150"/>
            <Button Height="24" Click="Return_Click" Content="Отметить возврат" Width="150"/>
            <Button Height="24" Click="Delete_Click" Content="Удалить запись" Width="150"/>
        </StackPanel>
    </Grid>
</Page>
